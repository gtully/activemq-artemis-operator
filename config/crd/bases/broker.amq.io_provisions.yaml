
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  name: provisions.broker.amq.io
spec:
  group: broker.amq.io
  names:
    kind: Provision
    listKind: ProvisionList
    plural: provisions
    singular: provision
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Provision is the Schema for the provisions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ProvisionSpec defines the desired state of Provision
            properties:
              nodeType:
                description: the hardware - the source of truth for some of the tradeoffs
                  is this a real or kube thing?
                items:
                  type: string
                type: array
              solutionType:
                description: a solution category
                items:
                  type: string
                type: array
            type: object
          status:
            description: ProvisionStatus defines the observed state of Provision
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
